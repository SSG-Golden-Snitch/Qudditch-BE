<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.goldensnitch.qudditch.mapper.UserCustomerMapper">
<!-- 사용자 추가 -->
<insert id="insertUserCustomer" parameterType="com.goldensnitch.qudditch.dto.UserCustomer">
    INSERT INTO user_customer (email, password, name, state)
    VALUES (#{email}, #{password}, #{name}, #{state})
</insert>

<!-- 이메일로 사용자 조회 -->
<select id="findByEmail" parameterType="String" resultType="com.goldensnitch.qudditch.dto.UserCustomer">
    SELECT *
    FROM user_customer
    WHERE email = #{email}
</select>

<!-- 인증 코드로 사용자 조회 -->
<select id="findByVerificationCode" parameterType="String" resultType="com.goldensnitch.qudditch.dto.UserCustomer">
    SELECT *
    FROM user_customer
    WHERE verification_code = #{verificationCode}
</select>

<!-- 사용자 정보 업데이트 -->
<update id="updateUserCustomer" parameterType="com.goldensnitch.qudditch.dto.UserCustomer">
    UPDATE user_customer
    SET email = #{email}, password = #{password}, name = #{name}, state = #{state}, modifiedAt = NOW()
    WHERE id = #{id}
</update>

<!-- 사용자 삭제 -->
<delete id="deleteUser" parameterType="int">
    DELETE FROM user_customer WHERE id = #{id}
</delete>
</mapper>